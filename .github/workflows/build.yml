name: runnertesting

on:
  workflow_dispatch:

jobs:
  sarguna:
    name: runnertesting
    runs-on: self-hosted
    steps:
      - name: Test runner
        run: echo "Manual run started on self-hosted runner"
