name: PAP Build Deploy #It appears in the GitHub Actions UI as the workflow name, It helps humans identify this workflow among many workflows.


on:
  workflow_dispatch:

jobs:
  sarguna:
    name: runnertesting
    runs-on: self-hosted
    steps:
      - name: Test runner
        run: echo "Manual sarguna self-hosted runner"
