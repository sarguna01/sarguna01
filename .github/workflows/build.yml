name: PAP Build Deploy

on: push
  

jobs:
  sarguna:
    name: runnertesting
    runs-on: self-hosted
    steps:
      - name: Test runner
        run: echo "Manual run started on self-hosted runner"
